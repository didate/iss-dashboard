root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/iss-backend ."
  bin = "./tmp/iss-backend"
  delay = 1000
  exclude_dir = ["tmp", "data"]
  exclude_regex = ["_test\\.go$"]
  include_ext = ["go"]
  kill_delay = "0s"

[log]
  time = false

[misc]
  clean_on_exit = true
