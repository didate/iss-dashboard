node_modules/
dist/
# Keep the embed placeholder so `go build` works without a frontend build.
# The real Vite output overwrites this dir at Docker build time (not committed).
!backend/internal/webui/dist/
backend/internal/webui/dist/*
!backend/internal/webui/dist/index.html
.env
*.db
*.xlsx
backend/tmp/
backend/data/
.claude/output/

# Artefacts locaux (captures de vérification, binaires compilés, données de travail)
.DS_Store
*.tsbuildinfo
backend/fixdata
.verify-shots/
data/
